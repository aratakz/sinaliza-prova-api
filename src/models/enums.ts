export enum QuestionFieldType  {
    title = "title",
    support_data = "support_data",
    none = "none",
}

export enum AccessLevel {
    ADMIN = 1,
    PROFESSIONAL = 2,
    TEACHER = 3,
    STUDENT = 4,
}